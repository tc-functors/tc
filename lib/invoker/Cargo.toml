[package]
name = "invoker"
version.workspace = true
edition = "2024"

[dependencies]
serde = "1.0"
serde_json = "1.0"
serde_derive = "1.0"
open = "5.0.0"
kit = { path = "../kit" }
aws = { path = "../aws" }
compiler = { path = "../compiler" }
