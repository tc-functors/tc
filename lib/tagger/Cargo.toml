[package]
name = "tagger"
version = "0.0.1"
edition = "2021"

[dependencies]
serde = "1.0"
serde_json = "1.0"
serde_derive = "1.0"
regex = "1.9.1"
semver = "0.7.0"
kit = { path = "../kit" }
aws = { path = "../aws" }
notifier = { path = "../notifier" }
