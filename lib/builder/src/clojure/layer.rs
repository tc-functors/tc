
use compiler::spec::LangRuntime;

pub fn build(
    _dir: &str,
    _name: &str,
    _runtime: &LangRuntime,
    _pre: Vec<String>,
    _post: Vec<String>,
    _trace: bool,
) -> String {

    String::from("")
}
