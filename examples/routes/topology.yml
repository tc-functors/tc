name: example-routes
infra: ./infra

routes:
  /v1/foo/test:
    method: GET
    gateway: g1_{{sandbox}}
    function: foo
  /v1/bar/test:
    method: GET
    gateway: g2_{{sandbox}}
    function: foo
