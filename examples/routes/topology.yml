name: example-routes

nodes:
  ignore:
    - route-states
routes:
  /api/ping:
    method: GET
    function: ponger

  /api/poke:
    method: POST
    event: PokeEvent

events:
  PokeEvent:
    producer: default
    function: processor
