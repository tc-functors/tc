name: ppd
description: my python layer
runtime:
  lang: python3.10
  package_type: zip
  handler: handler.handler
  layers: []
build:
  pre:
    - yum install -y git
    - yum install -y gcc gcc-c++
  kind: Layer
