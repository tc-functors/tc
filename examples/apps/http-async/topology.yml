name: http-async

routes:
  /message:
    authorizer: authorizer
    method: POST
    function: handler

events:
  ReceivedMessage:
    channel: message-channel

channels:
  message-channel:
    function: transformer
