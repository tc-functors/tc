name: api-auth


routes:
  /auth/register:
    method: POST
    function: registrar
  /auth/login:
    method: POST
    function: authenticator
  /api/ping:
    method: GET
    authorizer: authorizer
    function: ponger

pages:
  webapp:
    dist: .
    dir: webapp
