name: todo

mutations:
  authorizer: "{{namespace}}_authorizer_{{sandbox}}"
  types:
    Task:
      id: String!
      title: String
      status: String

  resolvers:
    AddTask:
      function: handler
      input: Task
      output: Task
      subscribe: true
