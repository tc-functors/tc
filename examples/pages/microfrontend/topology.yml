name: microfrontend

pages:
  mfe:
    dist: .
    nodes:
      foo:
        route: /foo
        dir: foo
      bar:
        route: /bar
        dir: bar
      baz:
        route: /baz
        dir: baz
