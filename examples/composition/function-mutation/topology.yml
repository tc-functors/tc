name: x-function-mutation

functions:
  foo:
    mutation: processMessage

mutations:
  types:
    Message:
      id: String!
      text: String
  resolvers:
    processMessage:
      input: Message
      output: Message
      function: bar
      subscribe: true
