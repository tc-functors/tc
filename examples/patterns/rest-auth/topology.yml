name: example-rest-auth

routes:
  /api/ping:
    authorizer: cognito
    function: pinger
    method: GET

pages:
  app:
    dir: .
    dist: dist
    config_template: index.html
    skip_deploy: true
