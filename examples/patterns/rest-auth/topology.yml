name: example-rest-auth

routes:
  /api/ping:
    authorizer: cognito
    function: pinger
    method: GET

pages:
  app:
    dir: app
    bucket: test
    config_template: src/config.js
    skip_deploy: true
    build:
      - npm install --quiet --no-audit
      - npm run build
