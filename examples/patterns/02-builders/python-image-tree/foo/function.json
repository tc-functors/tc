{
  "name": "foo",
  "description": "foo example",
  "runtime": {
    "lang": "python3.10",
    "package_type": "image",
    "uri": "{{repo}}/code:foo-latest",
    "handler": "handler.handler"
  },

  "build": {
    "kind": "Image",
    "images": {
      "base": {
	"parent": "",
	"commands": [
	  "yum install -y git wget unzip",
	  "yum install -y gcc gcc-c++ libXext libSM libXrender",
	  "pip install -vv -r requirements.txt --target /build/python"
	]
      },
      "code": {
	"parent": "base",
	"commands": []
      }
    }
  }
}
